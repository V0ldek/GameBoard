@model GameBoard.Models.FriendSearch.FriendSearchResultViewModel

<div class="autocomplete-@Model.GroupId autocomplete-items">
    @foreach (var user in Model.Users)
    {
        <div class="autocomplete-item friend-result">
            @await Component.InvokeAsync("UserCard", new { user, miniature = true })
            <form asp-controller="Groups" asp-action="AddUserToGroup"
                  class="invite-form"
                  data-ajax="true" data-ajax-method="POST"
                  data-ajax-success="groupAddPopoverGenerator.generateSuccessPopover(document.querySelector('#group-add-button-@user.Id'), xhr)"
                  data-ajax-failure="groupAddPopoverGenerator.generateErrorPopover(document.querySelector('#group-add-button-@user.Id'), xhr)">
                <input readonly hidden name="userName" value="@user.UserName" />
                <input readonly hidden name="groupId" value="@Model.GroupId"/>
                <button id="group-add-button-@user.Id" class="btn btn-dark mt-1" type="submit" data-toggle="popover" data-placement="right">
                    <i class="fa fa-user-plus"></i>
                </button>
            </form>
        </div>
    }
</div>